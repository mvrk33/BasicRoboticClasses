import machine
import time

led_pin1 = machine.Pin(25, machine.Pin.OUT)
led_pin2 = machine.Pin(14, machine.Pin.OUT)

while True:
    led_pin1.value(1)
    led_pin2.value(0)
    time.sleep(0.5)
    led_pin1.value(0)
    led_pin2.value(1)
    time.sleep(0.5)
